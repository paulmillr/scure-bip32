{
  "name": "build",
  "private": true,
  "version": "1.0.0",
  "description": "Used to build a single file",
  "main": "input.js",
  "keywords": [],
  "type": "module",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@scure/bip32": "..",
    "esbuild": "0.20.1"
  },
  "scripts": {
    "build:release": "npx esbuild --bundle input.js --outfile=scure-bip32.js --global-name=scureBip32"
  }
}
